const { expect } = require('chai');
const path = require('path');
const fs = require('fs');
const yoloNodeJs = require('../../yolonode');
const helper = require('../helper');

describe('Image', () => {
    /*it('should call get_color() function', () => {
        let c = helper.randomInt();
        let x = helper.randomInt();
        let max = helper.randomInt();
        expect(yoloNodeJs.get_color(c,x ,max)).to.not.eql(undefined);
    })*/
})